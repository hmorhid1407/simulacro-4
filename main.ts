input.onButtonPressed(Button.A, function () {
    basic.showNumber(0)
    basic.showNumber(0)
    basic.showNumber(0)
})
let número = 0
if (número == 0) {
    basic.showNumber(0)
}
if (número < 0) {
    music.playMelody("D C D E D C D E ", 250)
}
